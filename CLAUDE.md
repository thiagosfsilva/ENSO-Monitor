# ENSO Monitor — Claude Instructions

## Python Environment

Always use the `enso-monitor` conda environment for this project:

```
C:\Users\Thiago\miniforge3\envs\enso-monitor\python.exe
```

Use this interpreter for all script execution, testing, and package checks.
Never use the system Python or the `NGS_mapv2` environment for this project.

## Project Summary

Dash dashboard monitoring Amazon river water levels at ANA stations.
See `environment.yml` to recreate the conda env and `requirements.txt` for
the pinned Render/GitHub Action dependencies — keep them in sync.
